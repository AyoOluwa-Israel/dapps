export type TRegister = {
  first_name: string;
  last_name: string;
};

export type TBookDetails = {
  first_name: string;
  last_name: string;
  full_name: string;
  id: string;
  is_accepted: boolean;
  date_created: string;
  sn?: number 
};

